[project]
name = "data-acquisition-cdc-lakehouse"
version = "0.1.0"
description = "Production-style CDC lakehouse portfolio project"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
